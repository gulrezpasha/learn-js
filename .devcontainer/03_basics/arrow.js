const user={
    username: "pasha",
    price:999,

    welcomemessage:function(){
        console.log(`${this.username},welcome to message`);
    }
}
user.welcomemessage();

// const val=(a1,a2)=>  a1+a2
const val=(a1,a2)=> ( a1+a2)


console.log(val(2,5))

// iffi when global scope polluted our cirrent inner scope or function

// Immediately Invoked Function Expressions (IIFE)




